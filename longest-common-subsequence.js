/*
 *  Dynamic Programming:
 *    Longest Common Subsequence problem
 */
function lcs(A,B) {
    var n = A.length;
    var m = B.length;
    
    var table = new Array();
    
    for(var i = 0; i < n; i++) {
        var row = new Array(); // prepare a table row
    }
    
    return result;
}
